services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: minha_api_trabalho
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: minha_api_trabalho
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
